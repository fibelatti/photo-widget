<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.INFO" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.HOME" />
        </intent>
    </queries>

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <uses-permission
        android:name="android.permission.ACCESS_NETWORK_STATE"
        tools:node="remove" />

    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE"
        tools:ignore="ForegroundServicesPolicy" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:name=".App"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="tiramisu">

        <activity
            android:name=".home.HomeActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <activity
            android:name=".preferences.WidgetDefaultsActivity"
            android:exported="false" />

        <activity
            android:name=".backup.PhotoWidgetBackupActivity"
            android:exported="false" />

        <activity
            android:name=".configure.PhotoWidgetConfigureActivity"
            android:enableOnBackInvokedCallback="false"
            android:exported="true">

            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <activity
            android:name=".configure.PhotoCropActivity"
            android:exported="false" />

        <activity
            android:name=".viewer.PhotoWidgetViewerActivity"
            android:exported="false"
            android:launchMode="singleInstance" />

        <activity
            android:name=".chooser.PhotoWidgetChooserActivity"
            android:enableOnBackInvokedCallback="false"
            android:exported="false"
            android:launchMode="singleInstance"
            android:theme="@style/AppTheme.TransparentActivity" />

        <activity
            android:name=".folder.PhotoWidgetAppFolderActivity"
            android:enableOnBackInvokedCallback="false"
            android:exported="false"
            android:launchMode="singleInstance"
            android:theme="@style/AppTheme.TransparentActivity" />

        <activity
            android:name=".widget.ToggleCyclingFeedbackActivity"
            android:enableOnBackInvokedCallback="false"
            android:exported="false"
            android:launchMode="singleInstance"
            android:theme="@style/AppTheme.TransparentActivity" />

        <activity
            android:name=".widget.HeadlessFeedbackActivity"
            android:enableOnBackInvokedCallback="false"
            android:exported="false"
            android:launchMode="singleInstance"
            android:theme="@style/AppTheme.TransparentActivity" />

        <activity
            android:name=".licenses.OssLicensesActivity"
            android:exported="false" />

        <service
            android:name=".platform.KeepAliveService"
            android:exported="false"
            android:foregroundServiceType="specialUse">

            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Optional, user-enabled Foreground Service to keep widgets working as
                expected. This is a widget maker app that sits mostly in the background. Users won't
                interact with the app unless they want to make a new widget, but they expect their
                existing widgets to work. Certain OEMs stop the app since it's mostly in the
                background, resulting in a frustrating user experience. Other foreground service
                types aren't a good fit for this use case." />
        </service>

        <receiver
            android:name=".widget.PhotoWidgetProvider"
            android:exported="true"
            android:label="@string/app_name">

            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/photo_widget_info" />
        </receiver>

        <receiver
            android:name=".configure.PhotoWidgetPinnedReceiver"
            android:exported="false" />

        <receiver
            android:name=".widget.PhotoWidgetRescheduleReceiver"
            android:exported="false">

            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="com.fibelatti.photowidget.action.RESCHEDULE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".widget.ExactRepeatingAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".platform.ConfigurationChangedReceiver"
            android:exported="false">

            <intent-filter>
                <action android:name="android.intent.action.CONFIGURATION_CHANGED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">

            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>

</manifest>
